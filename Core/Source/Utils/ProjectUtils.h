#pragma once

#include <string>

namespace TrackingTool
{
    namespace Utils
    {
        // Generates a random 6-character project code (A-Z, 0-9).
        std::string GenerateProjectCode();
    }
}
